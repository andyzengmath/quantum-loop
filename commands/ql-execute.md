---
description: Run the autonomous execution loop. Picks stories from the dependency DAG, implements with TDD, runs two-stage review gates, handles retries and error recovery.
---

Load and follow the instructions in the `execute` skill from the quantum-loop plugin. The skill file is at `skills/execute/SKILL.md` relative to the plugin root.
