# v0.10.4 Bundle Design — patch-tier (--max-parallel/--stale-timeout parity + subsumption-claim correction + US-003 standing-backlog conversion)

**Date:** 2026-05-01
**Source:** Operator-approved scope per `idea-stage/IDEA_REPORT_v37.md` v0.10.4+ candidate slate.
**Predecessor:** `docs/plans/2026-05-01-v0.10.3-bundle-design.md`.
**Branch:** `ql/v0.10.4-bundle`.
**Target version:** 0.10.3 → 0.10.4 (patch).
**Total effort:** ~1 hour.

## Context

3 explicit work items from `IDEA_REPORT_v37.md` v0.10.4+ slate:
1. `--max-parallel` + `--stale-timeout` integer-validation parity (architect + security flagged in v0.10.3 review).
2. Subsumption-claim wording correction in v0.10.3 retro/CHANGELOG (architect MEDIUM honesty improvement).
3. US-003 dogfood reclassification: per-cycle deferral → standing-backlog item.

## Stories

| ID | Theme | Files | Risk |
|---|---|---|---|
| US-001 | --max-parallel + --stale-timeout integer-validation (matches --max-iterations + --max-retries pattern). | `quantum-loop.sh` | LOW |
| US-002 | Honest framing correction in retro docs: "subsumed by current coverage" → "obsoleted-by-v0.9.0-rewrite; residual wiring-reachability gap accepted as low-risk". | `idea-stage/PIPELINE_REPORT_v37.md`, `CHANGELOG.md` (annotation), `docs/plans/2026-05-01-v0.10.3-bundle-design.md`, `tasks/prd-v0.10.3-bundle.md` | LOW |
| US-003 | Drop dogfood from per-cycle stories; add standing-backlog reference in CLAUDE.md `## Process patterns` section. | `CLAUDE.md` | LOW |
| US-004 | Multi-perspective post-merge review (13th application). | (review-only) | LOW |
| US-005 | Retrospective + IDEA_REPORT_v38 + CHANGELOG [0.10.4] + 4 manifest bumps. | `idea-stage/`, `CHANGELOG.md`, 4 manifests | LOW |

## Decision points

- **Q1:** `--max-parallel 0` accepted? **Decision:** No — 0 parallel agents = nothing to dispatch; degenerate config. Use `^[1-9][0-9]*$` (positive only).
- **Q2:** `--stale-timeout 0` accepted? **Decision:** No — 0 minute stale threshold makes no sense (every story would immediately be classified stale). Use `^[1-9][0-9]*$`.
- **Q3:** Retroactively annotate v0.10.3 CHANGELOG? **Decision:** Yes, with `[v0.10.4 honest-framing correction]` parenthetical noting the wording shift. Preserves historical record + clarifies for future readers.

## Wave plan

US-001/002/003 file-disjoint (or overlap only on CHANGELOG which is sequentially-safe). US-004 dependsOn first 3. US-005 dependsOn all.

## Success metrics

- All 5 stories first-attempt PASS.
- `--max-parallel notanumber` rejected; `--max-parallel 0` rejected; `--max-parallel 4` accepted.
- `--stale-timeout notanumber` rejected; `--stale-timeout 0` rejected; `--stale-timeout 30` accepted.
- v0.10.3 retro/CHANGELOG/PRD/design doc updated with honest framing.
- CLAUDE.md `## Process patterns` adds standing-backlog reference for dogfood.
- 32 consecutive LOW G30 self-validation.

## Non-goals

- No real-feature dogfood (now standing backlog; reactivates when operator queues a feature).
- No new architectural work.

## References

- v0.10.3 review findings (architect: subsumption + parity; code-reviewer: stale comment; security: parity gap).
- `idea-stage/IDEA_REPORT_v37.md` § "v0.10.4+ candidate slate".
- `quantum-loop.sh:208-215` — current `--max-parallel` + `--stale-timeout` argparse (no validation).
- `quantum-loop.sh:163-184` — `--max-iterations` + `--max-retries` reference pattern.
