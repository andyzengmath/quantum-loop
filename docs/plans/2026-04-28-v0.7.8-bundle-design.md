# Design: v0.7.8 — patch-tier reactive (N36 + N37 + retrospective)

**Date:** 2026-04-28
**Source:** `idea-stage/IDEA_REPORT_v18.md` v0.8.1 candidates
**Branch:** `ql/v0.7.8-bundle`
**Target version:** 0.7.8

## Stories

- **US-001 (N36):** Improve `runner_load` error message in `lib/runner.sh` to suggest the correct form when a path-like input is given (e.g., "did you mean 'codex' (a name)?")
- **US-002 (N37):** Deprecate `runners/manifest.example.yaml` — add header noting `runners/*.json` is the authoritative per-runner manifest source; this yaml example is documentation only.
- **US-003:** Retrospective + IDEA_REPORT_v19 + version bump 0.7.7 → 0.7.8.

## Risk

LOW. All edits are doc/error-message touchups; no behavior change.

## Next steps

Advisory hooks → execute → review → squash → tag v0.7.8.
